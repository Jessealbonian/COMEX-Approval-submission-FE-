// Development environment - swapped in by Angular's fileReplacements
// when running `ng serve` / `ng build --configuration development`.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};
